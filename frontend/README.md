# AI Resume Builder Frontend - Phase 1 Auth

This Angular app includes only login, registration, JWT storage, protected dashboard routing, auth guard, and HTTP interceptor.

## Install and Run

```bash
cd frontend
npm install
npm start
```

Open:

```text
http://localhost:4200
```

The frontend expects the backend API at:

```text
http://localhost:8000
```
